import styles from './Header.module.css'

// import logo from '../assets/Logo.jpeg'

export function Header() {
    return (
        <header className={styles.header}>
            {/* <img className={style.logo} src={logo} alt="Logo da marca"/>    */}
        </header>
    )
};